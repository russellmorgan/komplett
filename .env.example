# Production Firebase web app config (Firebase console → Project settings → Your apps).
# Not needed for `pnpm dev`, which always talks to the local emulators.
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_APP_ID=
